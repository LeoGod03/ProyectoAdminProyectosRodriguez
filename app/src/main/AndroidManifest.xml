<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoFinalBarraLatina"
        tools:targetApi="31">
        <activity
            android:name=".GUI.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GUI.AdminActivity" />
        <activity android:name=".GUI.EncargadoActivity" />
        <activity android:name=".GUI.OpcionesInventario" />
        <activity android:name=".GUI.OpcionesProductoPaquete" />
        <activity android:name=".GUI.ListaProductosPaquetes" />
        <activity android:name=".GUI.ListaPedidos" />
        <activity android:name=".GUI.IngredientesInventario" />
        <activity android:name=".GUI.UtensiliosInventario" />
        <activity android:name=".GUI.NuevoProducto" />
        <activity android:name=".GUI.NuevoPaquete" />
        <activity android:name=".GUI.OpcionesPedido" />
        <activity android:name=".GUI.NuevoPedido" />

    </application>

</manifest>